import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Swim Perfect Limited - Swimming Training & Pool Solutions",
  description: "Professional swimming training, pool construction, maintenance, and comprehensive aquatic solutions across Nigeria. Summer camp programs available.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}